public enum eItemLocation { shelf, cart, ground }
public enum eShelfFaceDirection { down, right, up, left }
public enum eGameState { loading, running, paused}

public enum eItemType 
{
    Toiletpaper,
    Soup
}

public enum eKeys
{
    none,
    pause,
    exit,
    shoppinglist
}